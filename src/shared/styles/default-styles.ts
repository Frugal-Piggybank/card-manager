import {StyleSheet} from 'react-native';

export const defaultStyles = StyleSheet.create({
  text: {
    fontSize: 20,
  },
  textInput: {
    fontSize: 20,
    borderBottomWidth: 1,
    marginTop: 15,
    paddingVertical: 10,
  },
});
