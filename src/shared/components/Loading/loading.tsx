import React, {FC} from 'react';
import {View, Text} from 'react-native';

const Loading: FC = () => {
  return (
    <View>
      <Text>Loading...</Text>
    </View>
  );
};

export default Loading;
